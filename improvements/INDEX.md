# Framework Improvement Proposals (IMP) — Index

Improvement proposals are human-reviewed framework-health artifacts governed by
**Gate G125** (`framework_health_evidence_gate`). An IMP records a verified gap
and a proposed remedy; it does **NOT** auto-mutate `bubbles/*`, `agents/*`, or
`bubbles/workflows.yaml`. Implementation flows through the owning agents/gates
named in each proposal's "Files to touch" section.

To add one: copy [`TEMPLATE.md`](TEMPLATE.md) to `improvements/IMP-NNN-<slug>.md`,
fill every `<...>` placeholder, add a row below, and leave **Status: PROPOSED**
until the repo owner approves.

## Status legend

| Status | Meaning |
|---|---|
| `PROPOSED` | Authored, awaiting owner review. No framework files changed. |
| `ACCEPTED` | Owner approved. Implementation may route to the named owners. |
| `IN PROGRESS` | One or more scopes landing. |
| `APPLIED` | All scopes landed and acceptance criteria met. |
| `REJECTED` | Owner declined. Retain the file for audit history. |
| `SUPERSEDED` | Replaced by a later IMP (name it). |

## Gap-code legend

Gap codes group findings by the surface they compromise. Reuse existing codes
where a new proposal addresses the same surface.

| Code | Surface |
|---|---|
| `EV-*` | Evidence integrity — what counts as proof of work |
| `HO-*` | Handoff / dispatch governance — agent-to-agent routing, subagent depth, platform control fields |
| `GF-*` | Goal fidelity - whether autonomous delivery remains inside the operator-approved outcome and work boundary |
| `SEC-*` | Security and supply chain — installer, dependency, scan gates |
| `COV-*` | Coverage measurement — which gates and selftests actually run |
| `COST-*` | Context and token economics — prompt bundle size, cost visibility |
| `PERF-*` | Validation performance — wall-clock cost of the gate chain |
| `REG-*` | Registry consistency — gate bands, generated blocks, surface parity |
| `DOC-*` | Documentation truth — published claims matching implementation |
| `WIP-*` | Work-in-progress durability — unfinished work crossing the session boundary |
| `LRN-*` | Framework self-learning — whether the lessons/skill-evolution loop can actually close |

## Proposals

| IMP | Title | Status | Surface | Gap codes | Date |
|---|---|---|---|---|---|
| IMP-028 | Orchestrator Context Architecture (COST-1 remainder) | CLOSED 2026-07-29 — findings folded into `agents/bubbles_shared/operating-baseline.md` (R3); target unreachable by the proposed reduction, dedup premise measured false | framework-health (G125) | COST-1 (reduction; supersedes IMP-027 SCOPE-6) | 2026-07-28 |
| IMP-030 | Controlled Technical Prose: term registry enforcement + prose-form governance | APPLIED 2026-08-01 — SCOPE-1/2/3/4 landed; SCOPE-5 (Gate G132) DEFERRED under its own entry condition, no gate registered | framework-health (G125) | REG-3, DOC-2 | 2026-08-01 |
| IMP-031 | Feature Reachability: close the orphaned-implementation hole | APPLIED 2026-08-01 — SCOPE-1/2/3/4/5/6/8/9 landed; SCOPE-7 (gate registration) DEFERRED under its own entry condition, no gate registered | framework-health (G125) | COV-3, REG-4, DOC-3 | 2026-08-01 |
| IMP-032 | Status-mirror invariant: name it, explain it at failure time, and give it a legal repair path | APPLIED 2026-08-03 — SCOPE-2a/2b/3/4a/4b/5 landed; SCOPE-1 WITHDRAWN (premise falsified against source — no `E009` precondition is a gate). SCOPE-2b's recorded blocker was false: `state-transition-guard.sh` already passes the resolver's `E009-*` code through to the existing `blockingCode` field, so `E009-STATUS-MIRROR` needed no new field, no schema change, and no downstream parser migration. SCOPE-4b landed as `bubbles/scripts/state-certification-reconcile.sh`, which never writes the mirror on request — it re-runs the transition guard against a candidate whose mirrors agree and writes `certification.status` only on a PASS verdict. EV-4 acceptance criterion NOT met: all six `research-lab` specs were measured through the tool and all six were refused at exit 3, because their statuses are genuinely ahead of their evidence; the divergence now yields a specific blocking code instead of a dead end, which was the ergonomics failure the proposal targeted | framework-health (G125) | EV-4 (not met), COV-4, DOC-4, ~~REG-5~~ | 2026-08-03 |
| IMP-033 | Session closeout and open-work durability: stop losing work at the session boundary | APPLIED 2026-08-02 — SCOPE-1/2/3/4/6/7 landed; SCOPE-5 WITHDRAWN (`bubbles.upkeep` sets `disable-model-invocation: true`, so the mode's only consumer is an operator typing it, making its whole delta over the shipped `cli.sh closeout` a second way to type the same thing — not worth a carve-out in a NON-NEGOTIABLE policy file); the incidental G125 index-row finding is carried as OW-001 in `.specify/memory/open-work.md` | framework-health (G125) | WIP-1, WIP-2, WIP-3, COV-5, EV-5 | 2026-08-02 |
| IMP-036 | Delivery efficiency: close the dispatch hole, instrument the gates, and cut the ceremony that buys nothing | APPLIED 2026-08-06 — all 8 scopes landed. SCOPE-5 always-on context 850→545 lines; SCOPE-4 gate-hit telemetry (first sample: 29 gates observed, 20 rejecting nothing); SCOPE-7 agent field is an enum with a per-repo ratchet; SCOPE-3 gate G133 refuses evidence that proves nothing; SCOPE-2 parent-expansion rate now counted; SCOPE-8 all 114 gates carry a git-derived `since`/`sinceDate` plus an advisory vintage guard; SCOPE-6 compact hash-verifiable evidence and a no-op-state-write prohibition; SCOPE-1 single-orchestrator routing. THREE scopes falsified part of their own proposal and were corrected rather than forced: SCOPE-5's "delete the propagation instruction" (the capability is fully wired and degrades correctly — only the `applyTo: "**"` header was the cost) and its agent-retirement bullet (all 7 candidates are registered and wired, WITHDRAWN); SCOPE-2's enforcement half (already built — the guard already required a registered orchestrator, a ≥20-char reason naming the missing capability, and a resolvable evidence ref, so only visibility was missing). SCOPE-1's second-hop diagnosis was CONFIRMED empirically before the contract was rewritten: a dispatched subagent has no dispatch tool at all. Two self-inflicted defects were caught by the repo's own gates and fixed (a hardcoded product name in a portable surface, and a ratchet baseline placed where downstream repos are forbidden to write). Outcome metrics are unmeasured by design and carried as OW-014; gate retirement deferred as OW-012; the enum baseline shrink as OW-013 | framework-health (G125) | HO-1, HO-2, EV-6, COV-6, COST-2, COST-3, REG-6, REG-7 | 2026-08-06 |
| IMP-034 | Skill Evolution Loop: close the input-starvation and paraphrase-blindness holes | APPLIED 2026-08-03 — SCOPE-1/2/3 landed; SCOPE-4 (session-store adapter for LRN-3) DEFERRED per the proposal's own recommendation, no adapter registered. R6 discharged deliberately: the G068 word-overlap mechanism was evaluated and NOT reused, because `stg_scenario_matches_dod()` freezes its behavior for adopted `SCN-*` IDs, its stopword policy intentionally keeps `user`/`system`/`should`/`must` significant (precisely what over-merges lesson prose), and it is guard-internal, so sourcing it would pull the transition guard's exit paths into the detector. Jaccard was measured insufficient on a realistic paraphrase pair (4 shared, 10 union = 0.40, below the 0.60 threshold, so the loop would stay broken); the overlap coefficient scores the same pair 4/6 = 0.67, so grouping divides shared tokens by the smaller token set with a 3-shared-token floor guarding R1. The selftest proves the knob is live: at `similarityThreshold: 1.0` the same paraphrase set produces no proposal | framework-health (G125) | LRN-1, LRN-2, DOC-5 | 2026-08-03 |
| IMP-037 | Evidence-Backed Experience Recall | APPLIED 2026-08-07 — SCOPE-1 through SCOPE-7 delivered. SCOPE-2 local lexical provider/index and anchored lesson admission complete; independent final validation: index 50 passed/0 failed, resolver 22/0, schema 67/0, adapter 20/0, skill-evolution PASS; portability, syntax, shellcheck warning/error floor, no-network scan, LF/modes, G125, diff, and diagnostics clean. Fixed S2-F1..S2-F4: exact packet ID coherence, explicit Markdown/YAML unsupported accounting, status/index reconciliation, and structured invalid UTF-8 refusal. SCOPE-3 `recall` CLI family (`search`, `read`, `status`, `freshness`, `sync`) complete: public bash twin `bubbles/scripts/experience-recall.sh`, rooted to the active repository and refusing a remembered repository path; centralized slug derivation in `bubbles/scripts/repo-slug.sh`, now consumed by `repo-binding-preflight.sh`, which previously duplicated that logic, with parity proven by the binding selftest; `recall sync` classified as an owned mutation in `bubbles/action-risk-registry.yaml` and the only mutating subcommand in the scope; operator rows in `docs/CHEATSHEET.md` recording that recall is advisory, repository-rooted, and opt-in, with `local-lexical` opt-in and `none` the non-blocking default; and closed exit codes (0 fresh, 3 stale, 4 unknown, 5 disabled; 1 provider/malformed; 2 usage), repository rooting, response validation, and the risk boundary recorded in `agents/bubbles_shared/experience-recall.md`; independent final validation: CLI 192 passed/0 failed, pre-tool risk gate 33/0, index 50/0, action-risk-registry lint OK, repo-binding preflight all cases passed. Fixed S3-F1..S3-F6, three of them high severity: provider JSON consumed without validation, terminal-control sequence injection in the text output path, a file symlink able to switch the resolved repository root, incorrect source/downstream root derivation, provider exit codes not propagated through the wrapper, and a `sync` that could leave a partial index on failure, now atomic. SCOPE-4 lifecycle, bounded export, and deletion complete: durable lifecycle ledger engine `bubbles/scripts/experience-recall-lifecycle.py` writing `lifecycle.jsonl` under an exclusive lock, with atomic paired writes, closed-transition validation, ledger sequence-continuity checks, and repository-containment checks on the ledger, the derived state, and the export destination; a 91-case destructive-path selftest `bubbles/scripts/experience-recall-lifecycle-selftest.sh`; new `delete`, `admit`, `lifecycle` (`set`/`list`), and `export` subcommands in `bubbles/scripts/experience-recall.sh`; lifecycle metadata on every record, `lifecycleCounts` in status, default search restricted to `admitted`, and `sync_index` re-applying the ledger over the rebuilt corpus so tombstones survive rebuild, in `bubbles/scripts/experience-recall-index.py`; explicit `owned_mutation` overrides for `delete`, `admit`, `lifecycle`, and `export` in `bubbles/action-risk-registry.yaml`, which previously only inherited from `defaultRiskClass`; and the derived recall store and lifecycle ledger under `.specify/runtime/experience-recall` recorded in `agents/bubbles_shared/artifact-ownership.md` as derived state, never source authority, owned by `bubbles.super` with destructive-path selftests owned by `bubbles.test`; independent final validation: lifecycle 91 passed/0 failed, CLI 248/0 (was 192), index 50/0, pre-tool risk gate 33/0, action-risk-registry lint OK, G125 framework-health-evidence lint OK, shellcheck 0 findings on all SCOPE-4 files with `cli.sh` unchanged at 18 pre-existing notes (18 at HEAD, 18 now), and no-network scan showing no network or package calls in the new code. Observable acceptance verified: superseded, expired, and deleted records are absent from default search but remain visible to `status` and `read` for audit; `status` reports a count for every lifecycle state; `sync` honors tombstones; export respects its limit and omits raw source bodies; and delete changes only derived recall state, leaving the source artifact byte-identical, with `transition()` computing the digest before and after and reporting `sourcePreserved`. Fixed S4-F1..S4-F2: `cli.sh` help drift, where SCOPE-4 shipped four subcommands the operator-facing inventory never listed and omitted exit 6; and a latent selftest-harness defect where `grep -qF "$expected"` parsed any expected string beginning with `-` as a grep option, silently making whole classes of option-diagnostic assertions unassertable, now POSIX-portable `grep -qF -e "$expected"` in all four assertion helpers. Non-tautology was proven by mutation testing on an isolated copy: reverting the `cli.sh` help inventory, removing the disabled-adapter guard from `run_transition`, dropping the explicit `--limit` requirement on export, letting a refused export still create its `--output` file, and dropping `reject_derived_control` from transition parsing produced 1, 8, 1, 1, and 2 failures respectively. SCOPE-5 thin read-only MCP tools over the bash twins complete: `search_experience`, `read_experience`, and `experience_recall_status` under `bubbles/mcp/tools/`, each shelling out to the existing `bubbles/scripts/experience-recall.sh` twin with its matching subcommand and returning that twin's stdout, stderr, and exit code verbatim, so the MCP surface adds no scoring, freshness, or source validation of its own; all three carry the read-only annotation set (`readOnlyHint`/`idempotentHint` true, `destructiveHint`/`openWorldHint` false); the catalog exposes no mutating recall subcommand, so `sync`, `delete`, `admit`, `lifecycle`, and `export` stay CLI-only under their existing action-risk classification; and the catalog loader fail-fasts when a recall tool points at a missing twin, so MCP/CLI parity cannot silently rot. Independent final validation: MCP server selftest T1-T26 all passing, 0 failing (26 cases, was 22 before SCOPE-5; T25 asserts in two parts, so the run prints 27 PASS lines), MCP trust-boundary 16 passed/0 failed, experience-recall CLI 248/0 and lifecycle 91/0 both unregressed, management-truth lint exit 0. Recall is now CLI-reachable and MCP-readable but is still not activated in orchestrators; bounded orchestrator consumption is SCOPE-6. SCOPE-6 activates bounded orchestrator consumption behind a mechanical authority firewall: `bubbles/scripts/result-envelope-validate.sh` now refuses any envelope citing a recall record id, the recall index directory, or a content-classified recall export in `evidenceRefs`, `toolCalls`, `evidence`, or `dodRef`, and refuses it in EVERY mode including `--advisory` because citing tier-4 advisory content as evidence is an authority breach rather than a schema defect; narrative fields such as `summary` are deliberately not scanned so honest disclosure of consultation is not punished; exports are classified by CONTENT rather than filename because `export --output` takes a caller-named path that a name rule would lose to `mv`; and the refused shapes are DERIVED at runtime from `experience-recall-index.py` via AST rather than restated, with the indexer-absent fallback literals pinned by selftest so they cannot drift unseen. New `bubbles/scripts/experience-recall-authority-selftest.sh` 18 passed/0 failed, mutation-tested 5/5 caught (advisory-bypass, dropped `toolCalls` field, drifted fallback pattern, disabled export classification, anchored-match evasion). Consumption contract added at `agents/bubbles_shared/operating-baseline.md` → Experience Recall Consumption and `agents/bubbles_shared/experience-recall.md` → Authorized Orchestrator Consumption, and wired into the four authorized orchestrators (`bubbles.workflow`, `bubbles.goal`, `bubbles.sprint`, `bubbles.iterate`): consume at ONE boundary per phase only AFTER binding validation and current-source loading, retain at most 5 hits, read at most 2 records, label the block `advisory recalled experience`, discard before any repository/tool/DoD/status/Skill/dispatch decision, and never restate unavailable recall as a clean history. Repository isolation is structural, not documentary: `--repo-root`, `--repository-alias`, and `--adapter` are refused (exit 2) because root and alias are derived from the installed twin, and `--limit` is bounded to 1..20 (exit 2 outside), all grounded by execution. Separately corrected a real coverage gap found during this scope: none of the six recall selftests had ever been registered in `framework-validate.sh` — nothing auto-discovers `*-selftest.sh` there — so ~516 assertions were green but executed by no gate; all six are now registered with timeouts at ~4x measured runtime. Independent final validation: recall suite 516 passed/0 failed total (resolver 22/0, adapter-contract 87/0, index 50/0, CLI 248/0, lifecycle 91/0, authority 18/0), result-envelope-validate selftest 12/0, registry-consistency exit 0, agent-roster-coverage 3/0, reference-existence-lint 93 markdown files clean, agnosticity-lint 626 portable files clean, shellcheck warning floor clean. SCOPE-7 replaces asserted retrieval quality with measured retrieval quality: a labeled corpus at `bubbles/eval/fixtures/experience-recall/corpus.json` (12 documents, 13 queries, 3 near-miss probes) is scored by `bubbles/scripts/experience-recall-eval-selftest.sh` on every framework validation, recording macro precision and recall at the configured result bound plus per-query full recall. The first measurement exposed two genuine provider defects rather than a threshold mismatch: macro precision was 0.54, and the pure-stopword query `the it was and problem` returned five confident hits because the summary template begins `Problem:` so that token sat in 100% of summaries. Rather than lower the threshold to match, the provider was fixed with two deterministic, dependency-free controls — a document-frequency guard that zeroes query tokens saturated across the admitted candidate set (engaging only at 8+ records, so small existing fixtures are untouched) and a relevance floor keeping only hits within half the top score (applied after sort, before the limit, so the top hit always survives and ties are kept). Result: macro precision 0.54 -> 1.00 with macro recall held at 1.00, the stopword query 5 hits -> 0, and no regression across the pre-existing 516 assertions. Thresholds recorded in the corpus are set just under measured (0.95/0.95) with `requirePerQueryFullRecall` as the sharper guard, because macro precision only falls to about 0.96 when a single query degrades. The eval also attacks the boundaries: cross-alias and second-repository isolation, anchor validity on every returned record (in-root, non-traversing, digest-matching, fresh), unanchored-legacy-lesson exclusion, transcript inadmissibility, structured stale refusal, lifecycle deletion leaving default search while the source artifact survives, prompt-injection inertness (injected directive text cannot relabel the repository, escalate authority, or add a field outside the closed result contract), and proof that search mutates no source, lesson, or skill file. One integration assertion is deliberately not a fixture restatement: every record id the provider actually mints must match the exact shape `result-envelope-validate.sh` refuses as evidence, so an id-format drift cannot silently blind the firewall. Mutation-tested 5/5 reachable regressions caught (DF guard disabled, DF guard never engaging, relevance floor removed, index freshness gate removed, lifecycle filter removed); the sixth mutation — removing the per-record freshness filter — survives because that branch is genuinely unreachable while the index-level freshness gate fires first, and that is now documented in source rather than papered over with a contrived test. The stale assertion was tightened during mutation testing after it was found to pass for the wrong reason: a refusal and an empty result are indistinguishable from the caller's side, so it now asserts the `index-stale` refusal identity. Capability registered in `bubbles/capability-ledger.yaml` as `experience-recall` state `shipped` with 8 named consumers (the gating `result-envelope-validate.sh`, `cli.sh`, `mcp/server.py`, the 4 orchestrators, and the operating baseline); `capability-consumer-freshness.sh` exit 0 across 23 shipped capabilities and 78 verified consumer paths. Documentation: new recipe `docs/recipes/evidence-backed-experience-recall.md`, catalog row 61, control-plane schema surface 18, and a CHANGELOG entry. Independent final validation: recall suite 540 passed/0 failed across 7 selftests (resolver 22, adapter-contract 87, index 50, CLI 248, lifecycle 91, authority 18, eval 24), capability-ledger selftest 0, capability-consumer-freshness 0, capability-freshness selftest 0, reference-existence-lint clean, agnosticity-lint clean, G125 lint OK. Commit identity is otherwise carried by Git history; the one hash recorded here is `50330b3`, an unrelated concurrent commit by another agent in this shared worktree that absorbed the `bubbles/scripts/cli.sh` hunks registering the `recall` command family. History was deliberately not rewritten, so that CLI registration is not part of the SCOPE-3 commit | framework-health (G125) | LRN-4, EV-7, REG-8 | 2026-08-07 |

| IMP-038 | Goal Fidelity and Bounded Autonomous Delivery | APPLIED 2026-08-09 — all 7 scopes landed. SCOPE-1 froze one immutable Goal Contract per mutable run with a shared outcome model; SCOPE-2 closed the boundary ABSENCE hole (`--strict` refuses an undeclared boundary, `--require-allowed-paths` an undeclared mutation surface) and derived the enforced boundary from the contract via `sync-boundary`; SCOPE-3 threaded goal identity through every transition behind one producer (`ref`) and one comparator (`verify-ref`); SCOPE-4 added `goalImpact` ORTHOGONAL to the unchanged G095 disposition set and made the envelope validator refuse a routed finding reported as addressed; SCOPE-5 built the executable phase-relevance resolver and moved the mandate into a shared doc all four runners load; SCOPE-6 registered gate G134 over six drift boundaries and repaired G070; SCOPE-7 added the adversarial coverage plus prompt-free telemetry. TWO defects were found and fixed while building on the earlier scopes rather than deferred: `classify_boundary_change()` compared boundaries by SET DIFFERENCE, so narrowing a glob to a file inside it read as a widening and was falsely refused — reach is now compared by COVERAGE using the resolver's own matching rules, with a cross-script agreement test that fails if either side drifts alone; and G070's registry entry credited `artifact-lint.sh` with a presence check that script has never contained, leaving the ordinary feature path unenforced while the entry read `enforcedBy: [ unbound ]`. Three self-inflicted defects were caught by the repo's own gates and fixed: an `eval` in a selftest (G034), real sibling-repository names in portable fixtures (agnosticity scan), and a directly-edited GENERATED gates block (gates-registry-selftest) | framework-health (G125) | GF-1, GF-2, GF-3, GF-4, GF-5, GF-6 | 2026-08-09 |
| IMP-039 | Runtime context economics: measure the real prompt cost, bound what re-enters context, and resolve the output-policy contradiction | APPLIED 2026-08-12 — all 7 scopes landed. SCOPE-1 split "what the command must PRODUCE" from "what RE-ENTERS context", made the bounded `evidence-capture.sh` block the default above 40 lines, taught it to lift failure-shaped lines out of the omitted region, and added `output-policy-coherence-guard.sh` so the contradiction cannot silently return; SCOPE-2 added the `bubbles/adapters/usage/` contract with `none` as default and NO estimation path, and renamed `costProxy` to `referenceClosureProxy` (with `bubbles.retro`'s "Context Cost" heading becoming "Reference Closure") so a reachability proxy stops reading as spend — the direct lesson of IMP-028; SCOPE-3 gave G128 four context-volume dimensions, all default-null so existing repos are unchanged, with byte dimensions measurable today and token dimensions skipped rather than guessed when no adapter is configured; SCOPE-4 bound compaction to the LIVE transcript via `contextBoundary {kind,checkpointId,at}` with `unavailable` always declarable, so G083 measures the thing it is named for; SCOPE-5 shipped `tool-grant-lint.sh` advisory with a `tool-grants.yaml` registry and narrowed `bubbles.workflow` from 9 families to 7; SCOPE-6 introduced the compact always-on kernel and narrowed the two largest shipped instructions off `applyTo: "**"`, cutting the always-on surface from 26,563 B to 10,259 B; SCOPE-7 recorded the 0.34% completion-token measurement that settles the verbosity question and shipped ONE bounded default plus a stamped, ceiling-bounded `--diagnostic` escalation instead of a second mode. Two enforcement gaps were closed while building rather than deferred: the coherence guard's first pattern matched only quantifier-first phrasing and let "untruncated output for every command" through (now matched in both orders), and nothing bounded the always-on surface at all — IMP-036 SCOPE-5 had already cut it once and it regrew — so `always-on-instruction-budget.sh` now enforces both an allowlist and a byte ceiling. NOTE: the identifier IMP-039 is ALSO carried by the delivered autonomy-posture work (gate G135), which has its own SCOPE-1 and SCOPE-7; references to this proposal carry a COST-*/EV-* gap tag to disambiguate. | framework-health (G125) | COST-4, COST-5, COST-6, COST-7, EV-7 | 2026-08-11 |
| IMP-040 | Complete Test Creation And Certification | APPLIED 2026-08-12 (v7.27.0) — 13 scopes: test-inventory + mutation adapters, exact scenario target resolution (BUG-030), obligation matrix, testMechanism with closed vocabularies, production-path fidelity, dependency-path coverage, non-vacuity/riskTier, shared-consumer parity, source-to-scenario impact (implementationRefs), human acceptance terminal gate G136 (BUG-029), `verify-changed-specs` + CI template (BUG-031), agent workflow hardening, 8-shape evaluation corpus | framework-health (G125) | COV-8, COV-9, COV-10, COV-11, COV-12, REG-8, EV-8 | 2026-08-12 |
| IMP-041 | Semantic Goal Boundaries And Expansion Approval | APPLIED 2026-08-13 (v7.27.0) — Goal Contract v2 semantic boundary, scenario contribution + planned-delta contract, canonical G134 boundary receipts, digest-bound architecture-expansion approval, bidirectional proportionality, finding admission control, convergence materiality brake, and a 16-case incident corpus | framework-health (G125) | GF-7, GF-8, GF-9, GF-10, GF-11, GF-12, GF-13, COV-13 | 2026-08-12 |
| IMP-042 | Assurance-Preserving Lightweight Execution And Framework Cleanup | SUPERSEDED 2026-08-16 by IMP-047 - delivered leaf validation, compaction, MCP repair, micro-fix admission, selftest scheduling, payload closure, canonical registries, telemetry source classes, and selected docs cleanup. Validation registry, cursor, context-manifest, transition DAG, and measurement residuals move to IMP-047. Bounded parallelism was declined on measured evidence | framework-health (G125) | PERF-1..5, WIP-4, COV-14..16, REG-9..12, COST-8, DOC-6, EV-9, HO-3 | 2026-08-13 |
| IMP-043 | Close The Learning Loop: Capture, Schedule, Consume | SUPERSEDED 2026-08-16 by IMP-047 - delivered learning disposition, closeout reachability, capture-time compaction, scaffold backfill, doctor advisory, recall sync, loop selftest, and corrected docs. Registry paths, toggle consumption, config template, and setup opt-in move to IMP-047 SCOPE-10 | framework-health (G125) | LRN-4, LRN-5, LRN-6, LRN-7, COV-18, DOC-8 | 2026-08-13 |
| IMP-044 | Measurement Truth: One Populated Telemetry Plane | SUPERSEDED 2026-08-16 by IMP-047 - delivered populated-store readers, producer-aware registry blocks, reference-closure naming, and merge-preserving config writes. Tracking flags, outcome evaluations, activity field semantics, lifecycle tests, stale-config advisory, and published-reader corrections move to IMP-047 SCOPE-9 and SCOPE-11 | framework-health (G125) | REG-13, REG-14, REG-15, DOC-7, COV-17 | 2026-08-13 |
| IMP-045 | Continuation posture — make `unattended` real, then give it one honest entrypoint | SUPERSEDED 2026-08-16 by IMP-047 - delivered unattended runner consumption, G135 parity, prompt-count authority, generated count checks, and durable no-shortcut decisions. Completion, per-iteration fidelity, G086 wording, docs, and the gated `/bubbles.continue` entrypoint move to IMP-047 SCOPE-8 through SCOPE-11 | framework-health (G125) | DOC-6, COV-14, REG-9, REG-10, COV-15, GF-14 | 2026-08-14 |
| IMP-046 | Validation Debt Ledger And Settlement Boundaries | SUPERSEDED 2026-08-16 by IMP-047 - proposed declared input closure, typed receipts, append-only debt, distinct `PASS`/`REUSED`/`DEFERRED` states, settlement boundaries, batch execution, and assurance binding. No implementation landed. The complete design moves to IMP-047 SCOPE-6, SCOPE-7, and SCOPE-12 | framework-health (G125) | PERF-6, PERF-7, PERF-8, EV-10, COV-19 | 2026-08-16 |
| IMP-047 | Outcome-First Framework Convergence: Spec-to-Live Delivery, Smart Validation, and System Truth | APPLIED 2026-08-17 — all 5 slices landed, each preceded by its prerequisite blocker repair (PD-04, PD-07, PD-11, PD-12): S-A `82675b8` truthful gate telemetry, recording `fired` and `prevented` as separate facts; S-B `c96839b` single report/bug-packet registry authorities with `report-section-autofix.sh` deleted; S-D `9e41da4` proof-obligation traits, acceptance authority separating automation readiness from human acceptance, micro-fix packet as default; S-C `cdfdeb4` receipt-derived scenario states that refuse a hand-written state, semantic evidence admission, BUG-033 opened; S-E `2362c45` generated validation-checks registry with per-check closure. The Retirement Rule held — the Delete list was executed, not deferred. FOUR items were REFUSED on evidence rather than forced: 86 never-observed gates NOT deleted (one telemetry sample does not prove a gate never fires), 23 reader-less report sections downgraded to `templateOnly` rather than deleted, `improvements/IMP-NNN.state.json` refused because it would repeat the OW-016 drift it claimed to fix, and bounded parallelism left declined. Delivery also repaired 24 cold-gate regressions, a real `v5.3-selftest` deadlock, a `generate-validation-checks.sh` closure defect that silently degraded changed-only runs to the full suite, an agent bundle-budget breach re-seeded in `62ef790`, and a SIGPIPE race that made `diff-evidence-guard-selftest.sh` fail under load. BUG-032 stays `in_progress`: closing it needs a human G136 acceptance record, operator-only by the same design PD-12 delivered | framework-health (G125) | PERF-1..8, WIP-4, COV-14..19, REG-9..15, COST-8, DOC-6..8, EV-9..10, HO-3, LRN-4..7, GF-14 | 2026-08-16 |
| IMP-048 | In-Session Review Loop, Execution Receipts, and Bounded Session Lifecycle | APPLIED 2026-08-18 — all 8 scopes landed. SCOPE-2 `ccb7a92` made HOST-101 a mechanism: `dispatch-receipt.sh` records a receipt for EVERY dispatch and DERIVES the consequence — only a valid envelope advances a phase, retry budgets are per-class so an infrastructure fault cannot eat the defect-remediation budget, and REPEAT_INFRASTRUCTURE is derived and refused when asserted (89 assertions). SCOPE-3 `bd9a692` extended phase identity one level down to leaf receipts: an accepted leaf is not replayed, a changed owner invalidates only covering leaves, a timed-out leaf is UNRESOLVED and never RED evidence; the occurrence model was NOT duplicated — it moved into `occurrence-identity-lib.sh` sourced by both consumers, declining the BUG-004 mistake (70 assertions; phase-coordinator 21/21 proves the extraction preserved behaviour). SCOPE-1 `0461dd3` shipped the in-session review loop: Class A adjusts free and writes nothing, Class B needs recurrence >=2 and caps at 3 promotions, Class C dedupes until a metric worsens 25%, and the review is mechanically forbidden from writing `bubbles/`, `agents/` or `workflows.yaml`; `reviewCompliance` was added to the envelope schema as OPTIONAL so existing envelopes stay valid (95 assertions). SCOPE-6/7 `87a8801` stopped session controls being inert: per-mode `sessionBudget` defaults, a SOFT 70% boundary that recommends handoff without marking the spec blocked, and snapshot liveness where a gate reading an empty store reports UNMEASURED rather than PASS. SCOPE-4/5 `e430d00` raised evidence quality: a high-risk mutation declaration now needs an execution receipt naming a killed mutant (or a declared fallback reason), and an ordering claim must be asserted AT production return rather than proved by polling until it becomes true. SCOPE-8 `74a1ce5` bound the materiality brake to ad-hoc sessions at the first MUTABLE action; R7 is structural — a read outside the opening surface is never refused, proven by neutering the read exemption and watching 3 assertions go red. Every dimension defaults OFF per repo, so an unconfigured repository behaves exactly as before; the seven downstream per-repository adoption specs listed in the proposal remain the consumers. Per-scope detail lives in the commit messages | framework-health (G125) | LRN-8, HO-4, PERF-9, EV-11, EV-12, COST-9, WIP-5, GF-15 | 2026-08-17 |
| IMP-049 | Proportionate Self-Validation and Truthful Ceilings | APPLIED 2026-08-18 — 5 of 6 scopes landed; SCOPE-1 WITHDRAWN on measurement and the correction recorded rather than dropped. SCOPE-3 `fa550bc`: `framework-validate` took the machine-wide `flock` ~250 lines before parsing argv, so `--help`, `--list-tier` and even a typo returned "another run is already in progress" — the review preparing this IMP hit it. The lock now guards only invocations that execute checks; proven against a really-held lock that `--help`/`--list-tier` exit 0, an unknown flag exits 2 naming it, and an executing run still refuses. SCOPE-4 `5ecb0de`: 29 of 62 modes advertised a `statusCeiling` below `done` with no `transitionAudit`, which `transition-contract-resolver` refuses with E009-AUDIT-PROFILE-UNSUPPORTED — the ceilings were genuinely unreachable (BUG-017, the G087 defect class). Added `operational-completion-v1` for operational/docs terminals, asserting no completion class it cannot satisfy and keeping exclusions declarative so no packet is newly admitted; `workflow-registry-consistency` now fails the rule mechanically. SCOPE-5 `7e97303`: of 101 `script:` enforcer declarations, 11 named a script that never mentioned the gate. Ten were true and now cite it; the eleventh was false — G131 declared `claim-source-lint.sh`, which enforces G072 and contains "domain" zero times — and was removed, corroborated by the registry's own derived block. Also fixed `gate-enforcement-selftest` silently SKIPping (0 → 15 assertions). SCOPE-6 `1b974cd`: 15 wording assertions moved to an advisory lint, none deleted, incl. one gating releases on generated HTML; rewording a doc now leaves the gate green while the advisory reports drift, and deleting the concept still fails. SCOPE-2 `cc91ddb`: `release-check` may reuse a digest-keyed `framework-validate` receipt, opt-in and off by default, verdict read from exit code not wording; 8 refusal paths verified and a one-byte change flips accept to refuse. SCOPE-1 `75cd619` withdrawn: the rule was unsafe (`v7-selftest.sh` names no root identifier yet fails 5 ways under a bare tree, because `mode-resolver.sh` resolves the root itself) and the saving was illusory — 2.376s, not the projected ~790s, because layout-blind selftests are the cheap ones. | framework-health (G125) | PERF-10, PERF-11, PERF-12, REG-16, COV-20, COST-10 | 2026-08-18 |
