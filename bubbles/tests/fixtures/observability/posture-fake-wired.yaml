# Posture fixture: FAKE-WIRED. posture is wired but EVERY signal on BOTH
# planes is none (no usable evidence path).
# Expectation: rejected as fake-wired by the SCOPE-2 posture guard (G098).
traceContracts:
  observability:
    schemaVersion: 1
    posture: wired
    policy:
      undeclaredPosture: warn
    decision:
      decidedAt: 2026-06-11
      decidedBy: operator
      decisionSource: "bubbles.setup focus: observability"
      lastReviewedAt: 2026-06-11
    endpoints:
      validate:
        alerts: { adapter: none }
        sloBurn: { adapter: none }
        errorRate: { adapter: none }
        deployImpact: { adapter: none }
      operate:
        alerts: { adapter: none }
        sloBurn: { adapter: none }
        errorRate: { adapter: none }
        deployImpact: { adapter: none }
    slos:
      gateway.request:
        latencyP99Ms: 50
        errorRatePct: 0.1
        availabilityPct: 99.9
  workflows:
    booking.create:
      requiredSpans:
        - name: http.request
          attributes: [trace_id, booking.id]
      slo: gateway.request
