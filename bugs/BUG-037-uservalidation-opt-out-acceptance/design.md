# Design: BUG-037 — Opt-Out User Acceptance

> **Status of this document.** Design phase COMPLETE. `bubbles.design` confirmed
> the root cause against the files, completed the § 2.5 affected-surface sweep,
> and closed decisions D-1 through D-5 in § 3. Nothing here is implemented.
> Implementation is dispatched to `bubbles.implement` starting at Scope 1.

---

## 0. Design Brief

**Current state.** `uservalidation.md` implements opt-in acceptance. The
template ships every `## Checklist` item unchecked, and Gate G136 (Check 43 in
`guards/tail-delegated-gates.sh`) refuses a `done` transition unless every item
is checked AND a separately authored `## Human Acceptance Record` is present.
The contract lives in `bubbles/registry/acceptance-authority.yaml` and is read
by the shared `bubbles/scripts/acceptance-authority-lib.sh`, which both
`artifact-lint.sh` and the guard source. Four prose surfaces still describe the
pre-`9e41da4` contract and are false at HEAD.

**Target state.** Opt-out acceptance, per the owner's standing requirement. The
checklist ships CHECKED; an unchecked item at terminal still blocks as a
user-reported regression; the acceptance record becomes OPTIONAL and is no
longer required at terminal. Every governance surface is brought into agreement
with what actually executes.

**Patterns to follow.**
- Single-authority-plus-shared-reader, already in place:
  [acceptance-authority.yaml](../../bubbles/registry/acceptance-authority.yaml)
  is data, [acceptance-authority-lib.sh](../../bubbles/scripts/acceptance-authority-lib.sh)
  is the only parser, and both the lint and the guard source it. Change the data
  and the reader; never re-implement the parse at a call site.
- Guard-prints-and-stops, never guard-edits — Check 43's existing posture.
- Generated artifacts are regenerated by their generators, never hand-edited.

**Patterns to avoid.**
- The three-rule composition that PD-12 correctly diagnosed: template ships
  checked *because lint refuses otherwise*, and the terminal gate then reads
  those boxes as sign-off. The checked state must come from the CONTRACT
  (`shippedState: checked`), never from a lint refusal. This is why D-2 declines
  to restore the lint rule.
- Fixing a prose surface by text substitution without re-reading the script it
  describes. That is exactly how the four stale surfaces were produced.
- Treating `[ ]` authored by a template as equivalent to `[ ]` a user typed.
  They are indistinguishable in the file and must be separated by policy and
  date, not by inspection. See D-1.

**Resolved decisions.** All five are closed in § 3: D-1 dated grandfather,
documented not automated; D-2 lint stays shape-only; D-3 three-way split with
the permission and the prohibition assigned to different agents; D-4 BUG-032
out of scope with an exact routed note; D-5 keep the `PD12-*` prefix, retire
only `PD12-NO-RECORD`.

**Open questions.** None blocking implementation. Two items are routed to the
owner for awareness, not for a decision that gates this work — see § 8.

---

## 1. Root cause — CONFIRMED

Confirmed by `bubbles.design` against the files, not from the bug narrative.
The preliminary framing in § 1.1 is upheld, and the narrower alternative it
raised is **resolved in favour of the narrower reading** on new evidence
recorded in § 1.3.

### 1.1 Primary cause — a sound repair applied against an unrecorded requirement

Commit `9e41da4` identified a real fabrication vector: the template shipped
checked, lint *required* it to ship checked, and Gate G136 then read "every item
checked" as terminal human acceptance. Composed, those three rules said that
writing the template constituted the sign-off. That is a genuine defect and
PD-12 was right about it.

The repair chosen was to invert the default: ship unchecked, drop the lint
requirement, and add a separately authored `## Human Acceptance Record` as the
terminal proof. That repair optimises for un-forgeable proof of a human act.

The failure is that it made the owner's **normal case unreachable**. A user who
reviewed the delivered behavior and objects to nothing must now perform two
positive acts before the work can be called done. Nothing in the repository
recorded the owner's opt-out requirement at the time, so no surface contradicted
the change and no guard caught it.

### 1.2 Secondary cause — executable and descriptive surfaces updated on different clocks

`9e41da4` updated the template, the lint and the guard, and left the gate
registry, two shared agent modules, the skills catalog row and the changelog
describing the prior contract. Nothing in the repository forces a gate's
`description` to agree with the script that enforces it, so the drift was
silent.

**Confirmed against the files.** Each of the four reported surfaces still
carries both false assertions at HEAD:

| Surface | Line | Verified stale text |
|---|---|---|
| `agents/bubbles_shared/quality-gates.md` | 272 | "requires the checklist to carry at least ONE checked `[x]`"; "a checked-by-default template is legitimate" |
| `agents/bubbles_shared/test-core.md` | 304, 309 | same two assertions |
| `skills/bubbles-quality-gates-catalog/SKILL.md` | 57 | same two assertions |
| `CHANGELOG.md` | 1320 | the only G136 entry; carries both assertions; no PD-12 entry exists anywhere in the file |

`bubbles/registry/gates.yaml` `G136.description` (line 1074) carries the same
pair. Five surfaces, not four.

**Scope ruling on the mechanical-drift-check question the bug packet raised.**
A general "gate description must agree with its enforcer" check is **out of
scope for BUG-037** and is recorded as a candidate improvement. BUG-037 already
carries four scopes and a contract inversion; adding a new cross-cutting
conformance mechanism would widen it past the defect. What BUG-037 *does* ship
is the narrow version of that check scoped to this gate: S4-T1 (no surface
asserts the deleted lint requirement) and S4-T2 (`G136.description` names only
codes the guard can emit).

### 1.3 The narrower cause is the correct one — new evidence

§ 1.1 asked whether the true fabrication vector was the **lint requirement**
that forced the checked state, rather than the **checked default** itself. Two
surfaces found during the § 2.5 sweep settle it, and both are surfaces
`9e41da4` never touched:

| Surface | Line | Text at HEAD |
|---|---|---|
| `agents/bubbles.plan.agent.md` | 230 | "`uservalidation.md` — Create with checked-by-default baseline template (`- [x]` items)." |
| `docs/guides/FRAMEWORK_CONCEPTS.md` | 59 | "Human acceptance checklist — **users uncheck items to report regressions**" |

`bubbles.plan` OWNS `uservalidation.md` creation, and its instruction has said
"checked by default" continuously since before `9e41da4` and still says it at
HEAD. `FRAMEWORK_CONCEPTS.md` describes the opt-out mechanism explicitly.
Neither was updated by `9e41da4`.

Two consequences follow.

1. The repository at HEAD is **internally contradictory in the authoring
   direction too**: the owning agent is told to author checked, the template it
   is told to use ships unchecked, and the terminal gate demands an artifact
   neither mentions. The stale-documentation defect is therefore not one-sided
   drift toward the old contract — it is drift in both directions from a change
   that touched only the executable surfaces.
2. The narrower reading is correct. The fabrication vector was the
   **composition**, and specifically the leg that made the checked state
   *mandatory at lint time*. Remove that leg and the checked default is a
   contract decision rather than a mechanically-forced fact. This is the
   evidentiary basis for D-2, and it materially changes Scope 2: the two
   surfaces above need **no change at all**, because opt-out is what they
   already describe.

Neither surface is in `state.json.allowedPaths`, and neither needs to be —
confirmed by reading them rather than by assuming.

---

## 2. Affected surface inventory

Ownership per `agents/bubbles_shared/artifact-ownership.md` and the framework's
module ownership. **`bubbles.design` must verify this inventory is complete
before implementation is dispatched — it was assembled by search, not by
exhaustive proof.**

### 2.1 Contract and executable surfaces (behavior change)

| Surface | Owner | Change required |
|---|---|---|
| `bubbles/registry/acceptance-authority.yaml` | framework registry | `acceptance-checklist.shippedState` → `checked`; `acceptance-record.requiredAtTerminal` → `false`; rewrite the file-header rationale, which currently argues the opposite position at length |
| `bubbles/scripts/acceptance-authority-lib.sh` | framework scripts | `bubbles_acceptance_terminal_verdict` drops the `PD12-NO-RECORD` emission; retains `PD12-UNCHECKED-ITEM`; `bubbles_acceptance_shape_verdict` unchanged (it already validates the record only when authored) |
| `bubbles/scripts/acceptance-authority-selftest.sh` | framework scripts | Cases covering the new terminal contract plus adversarial bounds |
| `bubbles/scripts/artifact-lint.sh` | framework scripts | **D-2 CLOSED: no rule change.** The checklist rules are retained exactly. Only the stale comment block at lines ~555-563 is rewritten, because it explains a PD-12 rationale that no longer holds |
| `bubbles/scripts/guards/tail-delegated-gates.sh` (Check 43) | framework guards | Refusal text and `info` lines currently explain the acceptance-record requirement; must state the opt-out contract instead |
| `bubbles/registry/gates.yaml` — `G136` | framework registry | `description` rewritten (GC-2); `enforcedBy` re-checked |
| `bubbles/registry/gates.yaml` — `G057` | framework registry | **D-3 CLOSED: amend, required.** `description` carries the three-way split and declares that only initial authoring is mechanically checked — its enforcer never reads `uservalidation.md` |
| `bubbles/registry/bug-packet.yaml` (line ~47) | framework registry | `uservalidation.md` purpose currently reads "Acceptance checklist shipped **UNCHECKED**" — must invert |

### 2.2 Template and agent-instruction surfaces

| Surface | Owner | Change required |
|---|---|---|
| `agents/bubbles_shared/feature-templates.md` — uservalidation template | framework agents | `## Checklist` ships `- [x]`; rules block rewritten; `bubbles.journey` rule reconciled per D-3 |
| `agents/bubbles_shared/quality-gates.md` (line ~272) | framework agents | Stale G136 text replaced (GC-3) |
| `agents/bubbles_shared/test-core.md` (lines ~300-310) | framework agents | Stale "Human Acceptance Is Terminal" section replaced (GC-3) |
| `skills/bubbles-quality-gates-catalog/SKILL.md` (G136 row, line ~57) | framework skills | Stale text replaced — **also stale today**, carrying the same false "requires at least ONE checked `[x]`" assertion (GC-3) |

### 2.3 Regression surfaces

| Surface | Owner | Change required |
|---|---|---|
| `bubbles/scripts/state-transition-guard-selftest.sh` (Check 43 cases, ~line 5104) | framework scripts | Cases updated to the new terminal contract; the BUG-029 case must be **kept**, not relaxed |
| `tests/regression/test_35_human_acceptance_terminal.sh` | framework tests | The BUG-029 fixtures stay; the PD-12 `PD12-NO-RECORD` assertion (line ~122) inverts to assert a fully checked list with no record now **passes**; a new adversarial case asserts an unchecked item still fails |

### 2.4 Documentation and generated surfaces

| Surface | Owner | Change required |
|---|---|---|
| `CHANGELOG.md` | framework docs | New entry for this fix; correction to the BUG-029 entry (line ~1320) that now states a superseded contract as current (GC-4) |
| `docs/generated/gate-coverage-map.md` | **generated** | Regenerate via `bubbles/scripts/generate-gate-coverage-map.sh` — do not hand-edit (GC-5) |
| `bubbles/registry/validation-checks.yaml` | **generated** | Regenerate via `bubbles/scripts/generate-validation-checks.sh`; carries the label `BUG-029 human acceptance terminal regression (G136)` at line ~38638 (GC-5) |
| `bubbles/release-manifest.json` | **generated** | Regenerate via `bubbles/scripts/generate-release-manifest.sh` (GC-5) |
| `improvements/INDEX.md` | framework docs | **D-4 CLOSED: correct one sentence, touch nothing under `bugs/BUG-032-*`.** The IMP-047 row names a G136 blocker this fix removes, and was never BUG-032's only blocker. Replacement text is specified verbatim in § 3 D-4 |
| `BUGS.md` | framework docs | Index entry for BUG-037 |

### 2.5 Verification sweep — COMPLETE

The bug phase listed these as "not yet ruled out". `bubbles.design` swept them
by reading each file for an acceptance-model claim, using the pattern set
`ship(s|ped) (un)?checked | defaults to checked | auto-check | acceptance record
| at least one checked | checked-by-default | human acceptance`. Result: **2
carry a claim that is already correct under the new model, 2 carry a neutral
mention, 12 carry no acceptance-model claim at all.** No entry needs a change.

| Surface | Finding | Disposition |
|---|---|---|
| `agents/bubbles.plan.agent.md` line 230 | "Create with checked-by-default baseline template (`- [x]` items)" — the OWNING agent already specifies the target state | **No change.** Correct under the new model; stale only against HEAD. See § 1.3. Not in `allowedPaths`; none needed. |
| `docs/guides/FRAMEWORK_CONCEPTS.md` line 59 | "users uncheck items to report regressions" — describes opt-out | **No change.** Correct under the new model. Not in `allowedPaths`; none needed. |
| `docs/CHEATSHEET.md` line 268 | "`uservalidation.md` is human-owned acceptance input. Automation findings do not toggle it." | **No change.** The mirroring prohibition survives D-3 verbatim. |
| `bubbles/cheatsheet/vocabulary.json` `human acceptance` | Same sentence as CHEATSHEET line 268 | **No change.** Same reason. |
| `bubbles/scripts/framework-validate.sh` line 1084 | Registers `test_35` under the label "BUG-029 human acceptance terminal regression (G136)" | **No change.** A registration label, not a model claim. The BUG-029 attribution stays accurate: AC-4 preserves that closure. |
| `bubbles/scripts/bug-packet-selftest.sh` | zero matches | Ruled out |
| `bubbles/scripts/transition-contract-resolver.sh` + selftest | zero matches | Ruled out |
| `bubbles/scripts/repo-drift-report.sh` | zero matches | Ruled out |
| `bubbles/agent-ownership.yaml` | zero matches | Ruled out |
| `docs/guides/AGENT_MANUAL.md` | zero matches | Ruled out |
| `docs/recipes/guided-journey.md` | zero matches | Ruled out |
| `docs/recipes/new-feature.md` | zero matches | Ruled out |
| `docs/recipes/plan-only.md` | zero matches | Ruled out |
| `docs/examples/rest-api-endpoint.example.md` | zero matches | Ruled out |
| `skills/bubbles-feature-template/SKILL.md` | zero matches | Ruled out |
| `skills/bubbles-artifact-ownership-routing/SKILL.md` | zero matches | Ruled out |
| `templates/copilot-instructions.md.tmpl` | zero matches | Ruled out |

The same sweep across all of `agents/`, `skills/`, `templates/` and `docs/`
returned exactly three hits outside the four known-stale surfaces:
`bubbles.plan.agent.md:230` and `bubbles.journey.agent.md:72,89`. The journey
hits are the "MUST NOT auto-check" rule, which D-3 keeps in force verbatim.

**Consequence for Scope 4 step 10.** The sweep is done. That step reduces to
re-running the same pattern set at the delivering commit and confirming the
result is unchanged, which is S4-T1.

---

## 3. Design decisions — CLOSED

All five are closed by `bubbles.design`. Each records its ruling, the evidence
behind it, and its consequence for the scope list. Where a ruling changed a
scope's Test Plan or DoD, [scopes.md](scopes.md) was updated to match.

### D-1 — Migration posture for existing artifacts — **CLOSED: (b), dated grandfather, documented not automated**

**Ruling.** The framework ships the new template, the inverted contract and the
new reader, plus a `CHANGELOG.md` **upgrade note** that states a cutover date.
It ships **no migration script**, performs **no bulk auto-check** in this
repository, and BUG-037 modifies **no `uservalidation.md` outside its own
packet**. Files whose checklist predates the delivering commit are legacy-shaped
and are re-authored into the new shape by their own packet's owner, as ordinary
work on that packet, before that packet's own terminal transition.

**Evidence.** The in-repository population is exactly five files, all
`in_progress`, none at a terminal transition:

| File | checked | unchecked | first added | shape |
|---|---|---|---|---|
| `bugs/BUG-032-…/uservalidation.md` | 7 | 0 | 2026-08-16 | pre-PD-12, checked |
| `bugs/BUG-033-…/uservalidation.md` | 0 | 4 | 2026-08-17 | PD-12, unchecked |
| `bugs/BUG-035-…/uservalidation.md` | 0 | 12 | 2026-08-18 | PD-12, unchecked |
| `bugs/BUG-036-…/uservalidation.md` | 0 | 5 | 2026-08-18 | PD-12, unchecked |
| `bugs/BUG-037-…/uservalidation.md` | 0 | 6 | this packet | PD-12, unchecked |

`9e41da4` landed 2026-08-17. There are no `specs/` packets in this repository —
Gate G085 forbids them here — so the population is the `bugs/` tree only.

**Rationale.**

The load-bearing distinction is this: **an unchecked box authored by a template
is not a user's uncheck.** AC-6 forbids re-checking an item *a user unchecked*.
It does not forbid authoring a fresh checklist in the shipped state. A blanket
rule "automation may never turn `[ ]` into `[x]`" would be strictly stricter
than AC-6 and would strand the four PD-12-shaped files permanently, because
nobody ever rejected anything in them.

That distinction is **not decidable from the file**. Both cases produce an
identical `- [ ]`, and the file carries no history. So it must be bounded by a
fact outside the file, and the only such fact available to every reader,
including a downstream operator, is a **date**. Hence (b).

Option (a), pure leave-as-is, was rejected because it converts four live packets
into apparent all-rejections and gives downstream operators no instruction at
all — the four files would each read as "the user rejected every behavior",
which is false and unrecoverable without guidance.

Option (c), an operator-run migration script, was rejected outright. A script
that checks boxes across files a user may already have seen cannot distinguish a
template-authored `[ ]` from a user's deliberate uncheck. Running it would
perform exactly the act AC-6 forbids, and being operator-invoked does not change
what it does to the file. The `bugs/` population is five files; the cost of
hand-authoring is trivial and the risk of the script is not.

**What the framework source repo can and cannot do.** It cannot rewrite
downstream repositories — it ships files, it does not reach into consumers. So
the migration posture splits cleanly:

| Shipped | Documented only |
|---|---|
| the checked template, `shippedState: checked`, the new reader, the amended gate | the cutover date, the "legacy files ship unchecked and will now read as rejections" warning, and the instruction that the packet owner re-authors before the user reviews, never after |

**Consequence for the scope list.** Scope 2 step 5 changes from "apply D-1's
migration posture" to two concrete obligations: author no migration script, and
touch no foreign `uservalidation.md`. Scope 4 gains the upgrade note as
changelog content. Added: Scope 2 DoD item and test **S2-T8** asserting the
delivering commit modified no `uservalidation.md` outside this packet.

---

### D-2 — Does `artifact-lint.sh` re-acquire a checked requirement? — **CLOSED: NO. Lint stays shape-only.**

**Ruling.** `artifact-lint.sh` does **not** restore a "must carry at least one
checked `[x]`" check. Its checklist rules are unchanged. The only edit to that
file is the stale comment block at lines ~555-563, which explains a rationale
that no longer holds. The mechanical backstop the old rule provided moves to a
**template↔registry agreement check** (S2-T2).

**Rationale — three grounds, all verified.**

1. **Lint runs during planning, and the all-unchecked state is legitimate
   there.** A packet mid-review can legitimately have a user unchecking items,
   up to and including all of them. A lint that demands at least one `[x]`
   refuses the user's own act of rejecting everything. AC-4 exists to surface
   that state at the terminal transition, which is where it means something —
   not to make it un-lintable while the user is still working.

2. **The duplicate buys nothing.** Gate G136 already enforces *all* items
   checked at terminal. That is strictly stronger than "at least one checked".
   There is no input the lint rule would catch that G136 does not already
   refuse at the moment the claim stops being provisional. The rule would add
   planning friction and zero assurance.

3. **It is the coupling leg, and restoring it restores the vector.** This is the
   decisive ground. The ≥1-checked lint rule is precisely what *forced* the
   template to ship checked, and the composition "lint demands checked → template
   ships checked → terminal gate reads checked as sign-off" is the fabrication
   vector PD-12 correctly diagnosed. Under the new model the checked state comes
   from a **contract** — `acceptance-checklist.shippedState: checked` in
   `acceptance-authority.yaml`, a recorded owner decision. Deriving the checked
   state from a declared contract is sound. Deriving it from a *lint refusal*
   re-creates a chain in which three independent rules jointly assert that a
   human acted, with no rule actually checking that a human did. Keep the
   contract; do not rebuild the coupling.

The prompt asked that a restoration be justified explicitly. This ruling
declines to restore, so no such justification is owed — which is the cleaner
outcome and the reason the narrower root cause in § 1.3 matters.

**What replaces the backstop.** The only thing the old rule actually bought was
detection of a template authored in the wrong state. **S2-T2** buys exactly that
and nothing more: it asserts the uservalidation template block in
`feature-templates.md` agrees with `acceptance-authority.yaml`'s section
headings **and** its `shippedState`. That refuses a wrongly-authored template
without ever refusing a legitimately all-unchecked user file — the precise
separation the old rule could not make.

**Consequence for the scope list.** Scope 2 step 2 becomes "comment-only edit;
no rule change". SCN-B037-007 stands as written and is now an assertion that
HEAD behavior is *retained*, not changed. **S2-T2 is upgraded from a
nice-to-have to a load-bearing test** and gains an adversarial partner **S2-T9**
(a fixture template whose block ships unchecked must FAIL S2-T2). Scope 2 DoD
updated: the D-2 item now reads as a retention-plus-comment obligation.

---

### D-3 — G057 and the `bubbles.journey` "never auto-checks" rule — **CLOSED: three-way split; the permission and the prohibition belong to DIFFERENT agents; G057's clause is narrowed and declared advisory**

**The three-way split, as required.**

| Act | Verdict | Mechanically enforceable? |
|---|---|---|
| Automation authors the **initial** checked state when the artifact is created | **PERMITTED**, and required by AC-1 | **Yes** — via the template↔registry check (S2-T2). |
| Automation **re-checks** an item the user unchecked | **FORBIDDEN** (AC-6) | **No.** Advisory agent-instruction only. |
| Automation **toggles** an item either way to mirror a test outcome | **FORBIDDEN** | **No.** Advisory agent-instruction only. |

**The honest statement about mechanism.** No file-level mechanism can
distinguish rows 1 and 2. Both produce a byte-identical `- [x]`, and
`uservalidation.md` carries no authorship or history of its own. Git history
cannot close the gap either: it distinguishes them only when the user's uncheck
was itself committed, and an agent that overwrites an *uncommitted* uncheck
leaves no trace anywhere. Rows 2 and 3 are therefore obligations on agent
behavior, enforced by instruction and by review, **not by a guard**. This is
recorded here rather than papered over with a rule no mechanism enforces.

**A verified fact that resolves most of the apparent conflict.** G057's sole
enforcer is `bubbles/scripts/guards/control-plane-checks.sh`, and that script
**never reads `uservalidation.md` at all** — zero occurrences. So the clause
"`uservalidation.md` … MUST NOT be toggled to mirror automation outcomes" is
*already* advisory prose sitting inside a gate description whose enforcer does
not implement it. Declaring it advisory is not a weakening; it is a correction
of a description that overstates what the gate does, which is exactly the class
of defect GC-2 and GC-3 exist to fix.

**The tension dissolves on agent ownership.** The permitted act and the
forbidden act belong to different agents, at different times:

- **`bubbles.plan`** authors `uservalidation.md` at packet creation. It has the
  AC-1 permission — and its instruction at `agents/bubbles.plan.agent.md:230`
  already says "Create with checked-by-default baseline template (`- [x]`
  items)". **No change to that agent is required.**
- **`bubbles.journey`** runs later, against a live product, on a file that
  already exists. It never has a legitimate reason to write a `[x]`. Its
  prohibition therefore stays **absolute and unqualified**, with no exception
  carved out of it.

That is the clean resolution: the exception the bug packet feared would have to
be added to the journey rule does not exist, because journey is never the
author.

**Rulings.**

- **`gates.yaml` G057.description** — amend. Replace the single clause with all
  three rows above, and state plainly that only the first is mechanically
  checked while the other two are agent-instruction obligations that this gate's
  enforcer does not verify. Keep the mirroring prohibition verbatim in substance.
- **`feature-templates.md` `bubbles.journey` rule** — keep in force. Restate it
  so it names the split and cites the correct owner of the authoring permission,
  but do **not** add an auto-check exception to it. Its substance is unchanged.
- **`agents/bubbles.journey.agent.md`** — no change. Its lines 72 and 89 are
  correct as written and it is not in `allowedPaths`; none is needed.
- **`agents/bubbles.plan.agent.md`** — no change. Already correct; not in
  `allowedPaths`; none is needed.

**Consequence for the scope list.** Scope 2 step 1's journey sub-bullet
simplifies to "restate, do not except". Scope 4 step 2 changes from conditional
("amend per D-3 if that decision requires it") to **required, with specified
content**. Added: Scope 4 DoD item that G057's description declares the advisory
status of rows 2 and 3 explicitly, and test **S4-T8** asserting G057's
description contains no claim its enforcer does not implement.

---

### D-4 — BUG-032's blocked state — **CLOSED: CONFIRMED out of scope; note specified verbatim**

**Ruling.** The bug packet's recommendation is **confirmed**. BUG-037 does not
touch anything under `bugs/BUG-032-…/`. It corrects only the one sentence in
`improvements/INDEX.md`, which is already in `allowedPaths`.

**Evidence — executed, not inferred.**

```
$ bash -c 'source bubbles/scripts/acceptance-authority-lib.sh; \
    bubbles_acceptance_terminal_verdict \
    bugs/BUG-032-planning-maturity-guard-false-positives/uservalidation.md; echo "EXIT=$?"'
PD12-NO-RECORD: no authored "## Human Acceptance Record"; checked boxes alone are not human acceptance, because a template used to ship them checked
EXIT=1
```

BUG-032's checklist is 7 checked, 0 unchecked. Its **only** G136 finding today is
`PD12-NO-RECORD` — the exact code this fix deletes. Its `state.json` reports
`status: in_progress`, `nextRequiredOwner: bubbles.docs`, with outstanding
G043/G101 documentation surfaces.

**The INDEX.md sentence is wrong in two ways after this fix**, which is why it
must be corrected rather than left:

1. The G136 acceptance-record requirement it names as the blocker no longer
   exists.
2. It was never BUG-032's sole blocker. Independent `bubbles.docs` work is
   outstanding and is unrelated to acceptance.

Correcting it is not "closing BUG-032 as a side effect" — BUG-032 stays
`in_progress` on its own merits. Leaving it uncorrected would state a
non-existent blocker as current, the same defect Half 2 of this bug is about.

**Exact note.** Replace, in the `improvements/INDEX.md` IMP-047 row, the
sentence beginning "BUG-032 stays `in_progress`: closing it needs a human G136
acceptance record, operator-only by the same design PD-12 delivered" with:

> BUG-032 stays `in_progress`, now for its own reasons only: its remaining work
> is the operator-approved G043/G101 documentation surfaces owned by
> `bubbles.docs` (`nextRequiredOwner: bubbles.docs`). The G136 acceptance-record
> requirement that PD-12 added, and that this row previously named as the
> blocker, was removed by BUG-037 when opt-out acceptance was restored.
> BUG-032's checklist was authored 2026-08-16 under the pre-PD-12
> checked-by-default template, so its checked state is legacy-shaped and MUST
> NOT be read as a fresh human acceptance.

The final clause is the honest part and is deliberate: see § 8, item 1.

**Consequence for the scope list.** Scope 4 step 8 becomes concrete with the
text above. Added: Scope 4 DoD item and test **S4-T9** asserting the delivering
commit modified no path under `bugs/BUG-032-`.

---

### D-5 — Failure-code naming — **CLOSED: KEEP the `PD12-*` prefix unrenamed; retire only `PD12-NO-RECORD`; record the prefix as historical**

**Ruling.** No rename, full or partial. `PD12-NO-RECORD` is removed from
`failureCodes` because no code path can emit it any more, and its retirement is
recorded in the `acceptance-authority.yaml` header so a reader who greps it in
an archived evidence block can find out what happened to it. The remaining six
codes keep their names and meanings unchanged.

**Evidence — complete reader inventory, executed.** `PD12-` occurs in exactly
six files outside this packet, all inside the acceptance surface:

| Reader | Occurrences | Effect of this fix |
|---|---|---|
| `bubbles/registry/acceptance-authority.yaml` | 7 | `PD12-NO-RECORD` removed from `failureCodes`; header note added; 6 retained verbatim |
| `bubbles/scripts/acceptance-authority-lib.sh` | 7 | the `PD12-NO-RECORD` emission is deleted; the 6 other emitters are untouched |
| `bubbles/scripts/acceptance-authority-selftest.sh` | 9 | the NO-RECORD case inverts; the rest unchanged |
| `bubbles/scripts/state-transition-guard-selftest.sh` | 1 | updated to the new terminal contract |
| `tests/regression/test_35_human_acceptance_terminal.sh` | 2 | the NO-RECORD assertion inverts |
| `bugs/BUG-037-…/` (6 artifacts) | 35 | narrative only |

Distinct codes at HEAD: `PD12-UNCHECKED-ITEM`, `PD12-NO-RECORD`,
`PD12-AUTOMATION-ACCEPTOR`, `PD12-METHOD-FIELD-MISSING`,
`PD12-RECORD-INCOMPLETE`, `PD12-METHOD-UNKNOWN`, `PD12-READINESS-NOT-CHECKBOX`.

**Rationale.**

- **Renaming is cheap in this repo and expensive outside it.** Every in-repo
  reader is a file this bug already edits, so a rename would cost almost
  nothing here. But `acceptance-authority-lib.sh` and `acceptance-authority.yaml`
  both ship downstream — confirmed at `bubbles/release-manifest.json` lines 14,
  15 and 558 — and Check 43 prints these codes into guard output that downstream
  `report.md` evidence blocks capture verbatim. A rename silently invalidates
  every downstream matcher and orphans every archived evidence block, for a
  purely cosmetic gain.
- **The prefix is not actually misnamed for what remains.** The one code most
  tightly bound to the reversed finding — `PD12-NO-RECORD`, the demand for a
  separately authored record — is the one this fix deletes. The surviving six
  all enforce rules PD-12 *introduced and this fix keeps*: unchecked items still
  block, the record is still validated when present, an agent still cannot be
  the acceptor, readiness bullets must still be checkboxes. Those six are
  PD-12's durable contribution, and naming them after it is accurate.
- **Partial rename rejected.** A mixed prefix set is worse than either pure
  option: a reader who greps `PD12-` would get an incomplete set and reasonably
  conclude the renamed codes do not exist.

**Consequence for the scope list.** Scope 1 step 1's "record the D-5 decision"
becomes concrete: remove one code, add the header note, retain six. Added:
Scope 1 test **S1-T10**, an adversarial closure check that the set of codes the
library can emit equals the set `failureCodes` declares. That catches both
halves of the likely mistake — deleting the emission but leaving the code
declared, and deleting the declaration while an emitter survives.

---

## 3.6 What the owner's model gives up

Recorded plainly, per the constraint that the cost be stated rather than
papered over. [spec.md](spec.md) § 5 records the decision; this section records
its **design-level** consequence.

**A checked box is no longer evidence that a human acted.** After this fix, Gate
G136 at a terminal transition proves exactly one thing: *no user recorded an
objection in this file*. That state is reached identically by "the user
reviewed the behavior and was satisfied" and by "nobody ever opened the file".
The gate cannot separate them, and nothing downstream of it can either.

**G136's role narrows from acceptance proof to rejection channel.** Before this
fix it attempted to assert that acceptance happened. After it, it asserts only
that rejection did not. That is a real reduction in assurance and it is the
price of the owner's requirement — a satisfied user performing zero acts is
incompatible with any proof that the user acted, by construction.

**BUG-032 is the concrete demonstration, not a hypothetical.** Its seven boxes
were authored on 2026-08-16 by the pre-PD-12 template. After this fix that file
satisfies the terminal gate. Nothing in the repository can tell it apart from a
file a human reviewed item by item. That is why D-4's note says so explicitly.

**What is NOT given up, and must not be weakened later:**

| Property | Preserved by |
|---|---|
| An unchecked item still refuses a terminal transition, and every one is named | AC-4; the BUG-029 closure shape is proven intact by S1-T3 and S3-T3 |
| The guard never edits the artifact | AC-5; proven by sha256 comparison in S3-T4 |
| An agent can never name itself acceptor when a record is present | AC-8; `^bubbles\.` prohibition retained, proven by S1-T4 |
| Automation readiness grants no acceptance | AC-9; proven by S1-T7 |
| A user's uncheck is never reversed by automation | AC-6; **advisory only** — see D-3's honest statement |

The last row is the only one with no mechanism behind it. It is listed here
rather than omitted, because a preserved property with no enforcement is a
different kind of claim from one with a test, and conflating the two would be
the same failure this bug is about.

---

## 4. Fix approach

Four sequential scopes, each independently testable. See
[scopes.md](scopes.md).

1. **Scope 1 — invert the contract and the shared reader.** The registry and
   `acceptance-authority-lib.sh` are the single authority; everything else
   reads them. Changing them first means every later scope is verifying against
   an already-correct source of truth.
2. **Scope 2 — template, lint, and authoring agents.** What gets written.
3. **Scope 3 — guard and regression surfaces.** What gets enforced, with the
   BUG-029 pin explicitly preserved and proven.
4. **Scope 4 — governance documentation and generated artifacts.** What gets
   described, including the four stale surfaces and the changelog.

Scope 1 must land before Scope 3, because Check 43 and the regression tests both
source the library that Scope 1 changes.

---

## 5. Alternatives considered

| Alternative | Why not |
|---|---|
| Keep opt-in; document the owner's requirement as unmet | Contradicts an explicit standing owner requirement. Not the agent's call. |
| Keep the acceptance record required but auto-populate it | Forbidden. An agent naming itself acceptor is exactly `PD12-AUTOMATION-ACCEPTOR`, and naming a human it never spoke to is fabrication. |
| Ship checked AND keep the record required at terminal | Strictly worse than either pure model: the user must still act, and the checked default reintroduces the fabrication vector without buying back the convenience. |
| Delete Gate G136 entirely | Reopens BUG-029. AC-4 is the load-bearing half and must survive. |
| Make the acceptance record required only for specific modes or risk tiers | Adds a conditional the owner did not ask for. If it is wanted later, it is a separate improvement with its own evidence. |
| Restore the ≥1-checked lint rule as a mechanical backstop for AC-1 | D-2. It rebuilds the coupling leg of the fabrication composition and refuses a user who legitimately unchecks everything. S2-T2 buys the only assurance it offered, without either cost. |
| Ship an operator-run migration script for legacy `uservalidation.md` files | D-1. It cannot distinguish a template-authored `[ ]` from a user's uncheck, so running it performs the act AC-6 forbids. Operator invocation does not change what it does to the file. |
| Rename `PD12-*` to a neutral prefix | D-5. The library and registry ship downstream and the codes appear verbatim in captured guard output; a rename orphans downstream matchers and archived evidence for a cosmetic gain. |
| Add a general "gate description must agree with its enforcer" conformance check | § 1.2. Correct in principle and out of scope here; BUG-037 already carries a contract inversion across four scopes. Recorded as a candidate improvement. S4-T1 and S4-T2 ship the version of it scoped to this gate. |

---

## 6. Regression design

Every scope carries at least one **adversarial** case — an input that passes
with the fix and fails if the fix is reverted or over-applied.

| Risk the fix introduces | Adversarial case |
|---|---|
| The inversion widens into "terminal never checks acceptance" | A checklist with one unchecked item still fails terminal, and the item is named |
| BUG-029 silently reopens | One checked plus five unchecked still fails, with all five named |
| Record validation is dropped along with the requirement | A present record naming `bubbles.validate` as `acceptedBy` still fails the shape verdict |
| `## Automation Readiness` is treated as acceptance | All readiness items checked, one checklist item unchecked → terminal still fails |
| The guard starts editing the file | Byte-compare `uservalidation.md` before and after a refusing guard run |
| A doc surface is fixed by text substitution without matching the code | Compare each G136-describing surface against `artifact-lint.sh` at the delivering commit |
| Lint relaxation widens into "lint no longer reads the checklist" | S2-T4 and S2-T5: a non-checkbox bullet and a zero-checkbox checklist both still FAIL lint |
| The template is authored in the wrong shipped state, with no lint rule left to catch it | S2-T2 plus its adversarial partner S2-T9: a fixture template block shipping `- [ ]` must fail the template↔registry agreement check |
| A failure code is declared but unemittable, or emitted but undeclared | S1-T10: the set the library can emit equals the set `failureCodes` declares |
| A foreign `uservalidation.md` is silently rewritten during the fix | S2-T8: the delivering commit modifies no `uservalidation.md` outside this packet |
| BUG-032 is closed as a side effect | S4-T9: the delivering commit modifies no path under `bugs/BUG-032-` |
| G057's description keeps claiming enforcement its enforcer does not perform | S4-T8: G057's description declares which of its three rules are mechanical and which are advisory |

---

## 7. Complexity Tracking

| Deviation from the simplest viable approach | Simpler alternative considered | Why rejected |
|---|---|---|
| Four scopes rather than one commit | Invert `shippedState`, delete the `PD12-NO-RECORD` emission, fix the prose, done | The change crosses a registry contract, a shared reader, a downstream-shipped template, a blocking guard, two selftests, a persistent regression test and three generated artifacts. A single scope would put the BUG-029 pin, the lint retention and the generated-artifact regeneration in one undifferentiated evidence block, and a partial revert could not be reasoned about. Scope 1 lands the authority first so every later scope verifies against an already-correct source. |
| Keeping `## Human Acceptance Record` and `## Automation Readiness` after removing the terminal requirement | Delete both sections; opt-out makes neither load-bearing | Explicitly forbidden by AC-7 and AC-9. The record still serves external UAT and compliance sign-off, and the readiness block still records that automation verified the behavior before a human's time was spent. Deleting them would discard capability to simplify a diff. |
| Adding S2-T2 as a load-bearing template↔registry check | Rely on the template being authored correctly | D-2 removes the only mechanical backstop for AC-1. Leaving no replacement would mean a wrongly-authored template ships silently — which is the failure mode `9e41da4` itself demonstrates, since it changed the template and left the owning agent's instruction contradicting it. |
| Correcting a sentence in `improvements/INDEX.md` about a different bug | Leave BUG-032's row untouched | D-4. The sentence names a blocker that this fix removes. Leaving it states a non-existent requirement as current, which is precisely the Half-2 defect class. Correcting the sentence is strictly narrower than touching BUG-032's packet, which this fix does not do. |

---

## 8. Routed to the owner — awareness, not a gate

Neither item blocks implementation. Both are recorded because they were found
by executing against the files and would otherwise surface later as surprises.

### 8.1 A legacy-checked artifact becomes terminal-eligible with no human act

`bugs/BUG-032-…/uservalidation.md` carries seven checked items authored
2026-08-16 by the pre-PD-12 checked-by-default template. Its only G136 finding
today is `PD12-NO-RECORD` (executed; exit 1). After this fix it satisfies the
terminal gate outright.

This is the § 3.6 trade made concrete rather than a defect in the fix — the
owner's model accepts exactly this. It is routed because **if the owner wants
BUG-032's acceptance re-confirmed by a human before it closes, that has to be an
explicit instruction inside BUG-032's own packet.** BUG-037 will not do it, and
after this fix nothing will refuse it automatically. D-4's note states the
legacy-shaped status in `improvements/INDEX.md` so the fact survives in writing.

### 8.2 HEAD is internally contradictory in the authoring direction, and the owner requirement was never the loser

`agents/bubbles.plan.agent.md:230` — the instruction for the agent that OWNS
`uservalidation.md` creation — has said "Create with checked-by-default baseline
template (`- [x]` items)" continuously through `9e41da4` and still says it.
`docs/guides/FRAMEWORK_CONCEPTS.md:59` likewise describes opt-out.

So the owner's requirement was recorded in the repository after all, in the two
places that describe *authoring* — it was simply never reconciled when the
executable surfaces inverted. This does not change any ruling; it strengthens
§ 1.3 and it means two fewer surfaces to change.

### 8.3 Nothing found contradicts the owner requirement

The § 2.5 sweep, the `agents/`+`skills/`+`templates/`+`docs/` sweep and the
G057 enforcer reading turned up no surface asserting a rule incompatible with
opt-out acceptance. The `bubbles.journey` absolute no-auto-check prohibition
looked like a conflict and is not one: journey never authors the artifact, so
D-3 keeps that rule unqualified.

The one genuine cost is not a contradiction but a deliberate trade, stated in
§ 3.6 and in [spec.md](spec.md) § 5: after this fix a checked box is no longer
evidence that a human acted, and G136 narrows from an acceptance proof to a
rejection channel.
