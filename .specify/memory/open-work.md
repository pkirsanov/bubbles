# Open Work Register

This file is COMMITTED on purpose. A record of open work that lives only in a
chat transcript, a terminal scrollback, or an uncommitted file is lost at
exactly the moment it is needed — when the session ends.

Render it with:

```
bash bubbles/scripts/cli.sh open-work
```

## What belongs here

Only **residue**: work that was noticed and never filed. It has no spec, no bug,
and no improvement entry, so nothing else in the repository knows it exists.

Rows for specs, bugs, and improvements are **derived on every run** from
`state.json` (via `work-tracker-project.sh`) and `improvements/INDEX.md`. Do not
author them here. Writing a status into this table that another artifact already
owns creates a second source of truth, and the two will disagree.

## Rules

- A residue row MUST carry both a `next-owner` and a `next-action`. A row nobody
  owns, or whose next step is "finish the thing", does not survive the next
  session and fails `open-work --lint`.
- `kind` must be `residue`. Any other value is a lint defect.
- `id` must be unique, so a row can be removed unambiguously when it closes.
- **Closed rows are DELETED, not tombstoned.** A row disappears when its work is
  done or when it graduates into a spec, bug, or improvement — at which point
  the derived projection covers it. This table answers "what is still open"; a
  growing tail of closed rows destroys that answer. Git history is the audit
  trail for what was removed and when.

## Residue

| id | title | kind | ref | state | next-owner | next-action | opened | last-seen |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OW-002 | release-hygiene fails on BOTH ubuntu and macOS at the same step; five root causes are now attributed and fixed including both ubuntu failures, the remaining macOS FAILs are unattributed | residue | GitHub Actions run 30779788232 on commit ad46b40, job release-hygiene-macos | open | framework-validate maintainer | NEXT STEP: re-run the CI workflow, then read the per-step conclusions through the PUBLIC REST API - `curl -sS https://api.github.com/repos/pkirsanov/bubbles/actions/runs/<run-id>/jobs` needs no credential at all. Two of the failures are fixed as of 2026-08-07, so the surviving set should be smaller; attribute each survivor INDIVIDUALLY, and reproduce it locally with the BSD userland simulator before forming a hypothesis. DURABLE IMPROVEMENT LANDED 2026-08-08: the release-hygiene step now emits each FAIL as a `::error::` workflow command. `bubbles_ci_annotate_failure` in `guard-lib.sh` is wired into every FAIL site in `framework-validate.sh` (run_check, discovered-sweep) and `release-check.sh` (run_check, --fix), gated on GITHUB_ACTIONS so local output is byte-identical to before, and escaped per GitHub's %25/%0D/%0A rules so a label carrying `%` or a newline cannot truncate or forge a command. Pinned by `ci-annotation-emitter-selftest.sh` (14 assertions, enumerated in framework-validate.sh), whose non-tautological cases are: no annotation when GITHUB_ACTIONS is unset, no annotation for a PASSING check, and a generalizing check that EVERY `echo "FAIL: ` site in both scripts is annotated so a future unannotated site is caught. Verified to fail when the emitter is neutered (3 failures), when the call site is removed (4 failures, naming release-check.sh:46), and when the gate is removed (1 failure). Annotations ARE publicly readable - `GET /repos/pkirsanov/bubbles/check-runs/<job-id>/annotations` answered unauthenticated on 2026-08-07 - so from the NEXT run onward every surviving macOS FAIL is attributable from any machine with zero credentials, without touching the 403-gated raw log. WHAT REMAINS IS OPERATOR-SIDE ONLY: push, let `release-hygiene-macos` run, then read the annotations endpoint and attribute each survivor INDIVIDUALLY. UNBLOCK CORRECTED 2026-08-07 (measured, second pass): the recorded blocker was FALSE and is withdrawn. Read access was never denied. `pkirsanov/bubbles` is public and the REST API answers UNAUTHENTICATED - the workflows, runs, jobs and per-step endpoints all returned data with no token. What obstructed access was the client, not GitHub: `gh` returned 401 because an INVALID `GH_TOKEN` was being SENT, and with no token at all it refuses outright with exit 4 and "To get started with GitHub CLI, please run: gh auth login". Use `curl` against `api.github.com`, and do not conclude that a credential is required for run, job or step metadata. The stale `GH_TOKEN` export has since been removed from `~/.bashrc`, so `gh auth login` now proceeds normally. ONLY THE RAW LOG DOWNLOAD IS PRIVILEGED: `GET /actions/jobs/<id>/logs` returns 403 "Must have admin rights to Repository.", not 401, so a valid non-admin token would not help either - only the repo owner can pull raw logs. Per-step conclusions are public and were sufficient to attribute both ubuntu failures without reading one log line. REFUTED, bash 3.2: the macOS job's step 3 is literally "Install bash 4+" (`brew install bash`, then `export PATH="$(brew --prefix)/bin:$PATH"` for every later step), so macOS is NOT running bash 3.2 in CI; do not re-test that link. REFUTED, macOS-exclusivity: on run 31202950441 (commit 44ba0ce) BOTH `release-hygiene` on ubuntu and `release-hygiene-macos` fail, and both fail at the SAME step - "Run release hygiene", which is `bash bubbles/scripts/cli.sh release-check`. The "28 macOS FAILs against 2 on ubuntu" framing hid that the two ubuntu failures share that step. STILL TRUE, carried over from the withdrawn note: `release-hygiene-macos` in `.github/workflows/agnosticity.yml` is gated `if: github.event_name == 'push'`, so it runs on every push to main and fresh macOS results already exist - no macOS hardware and no waiting is required. ATTRIBUTED AND FIXED 2026-08-07: both ubuntu failures, and ONLY those two. (1) `gate-bands` drift - the gate registry had reached G133 while four derived surfaces still said G132. Fixed in `df65ef4`, which authored a real curated G133 catalog entry rather than bumping a number to silence the warning; companion `1be85ad` corrected the freshness guard's boundary detection, because a boundary is a section marker and not a word occurring inside a sentence. (2) class-1 raw-timeout false positive in `framework-validate.sh` - BUG-021's regression test requires that file to scan clean under the macOS portability guard, and it had failed since `a0818c7` (the OW-009 stall fix), which added a case pattern matching the check NAME "guard-lib timeout fallback"; class-1 read that quoted string as a `timeout` invocation when it is a case label, not a command. Fixed in `62a17db` with the guard's own `# portable-ok:` pragma, chosen over renaming the check (which would desynchronise the selftest output from the check it names) and over widening class-1 (which would blind it to a genuine `timeout 30 cmd` inside a quoted string). Confirmed pre-existing rather than introduced by that session: the tree at `8b2ac83`, before it, fails the same check when scanned directly. Nothing else was fixed, and every check listed below as unattributed is still open. ATTRIBUTED AND FIXED 2026-08-03: bare `timeout` exiting 127 (13 call sites across 4 selftests), GNU-only `sed -i` in workflow-planning-provenance-selftest, and a shallow-clone `git show 86fc700` that failed evidence-admission on BOTH platforms. STILL UNATTRIBUTED: capability-ledger hermetic regeneration, capability freshness, gate-ID grep, MCP HTTP transport, MCP trust-boundary, context compactor, competitive docs, envelope truncation checks, repository work-boundary aggregate, and the runtime-concurrency check reporting "fixed state-snapshot lost an update in 1/10 rounds". Do not assume the remainder share a cause. Seven of them - capability-ledger, capability freshness, MCP HTTP transport, MCP trust-boundary, context compactor, repository work-boundary and runtime-concurrency - are ruled out for userland flag divergence as of 2026-08-07 (see RULED OUT 2026-08-07 below), but they remain unattributed as CI failures. RULED OUT 2026-08-06: OW-009's root cause (the timeout fallback watchdog holding the caller's stdout pipe and orphaning a long sleep) was tested as a shared explanation for these nine and REFUTED — none of capability-ledger, capability-freshness, mcp-http, mcp-trust, context-compactor, competitive-docs, envelope-truncation, work-boundary or state-snapshot calls bubbles_run_with_timeout, and run_check does not wrap checks in it either (only 3 call sites pass it explicitly). The fallback is macOS-only and so are these failures, which made it a plausible shared cause; it is not one. Do not re-test that link. RULED OUT 2026-08-07, userland flag divergence for seven of the checks above: the `capability-ledger`, `capability-freshness`, `mcp-http-transport`, `mcp-trust-boundary`, `context-compactor`, `work-boundary-resolve` and `runtime-concurrency` selftests ALL pass under full BSD-userland simulation on a sealed PATH, and those files contain zero divergent constructs directly. The simulation was proven to bite by a control probe run through the identical invocation path: inside it `timeout` is absent and a bare `sed -i` is rejected by the simulator's own message, so passing there is a real negative result and not a silently inert harness. Do not re-test that link for these seven; they are still unattributed, for some other reason. NEW CAPABILITY 2026-08-07: `bubbles/scripts/bsd-userland-sim.sh` (commit `dcb7a62`, 60-assertion selftest, wired into `framework-validate.sh`) lets a Linux host reproduce a macOS failure, which closes the reproduction gap that is precisely why these failures sat unattributed - the pre-existing PATH shim only mapped gsed to sed and gtimeout to timeout, which is macOS-to-GNU, and nothing let a Linux host run BSD-shaped userland. Use `SEALED="$(bash bubbles/scripts/bsd-userland-sim.sh --sealed)"` then `env PATH="$SEALED" bash <selftest>`. ALSO FIXED 2026-08-07 (real defects, but none of them a CI failure listed here): twenty GNU-only `mktemp` sites, thirteen in `583923a` and seven more in `fb1de97`, plus the blind spot that hid them, closed with portability guard classes 14 (`mktemp -p` and `--tmpdir`, walking the whole flag run so `-d -p` is caught) and 15 (non-trailing X run) in `52377a1`. SECURITY 2026-08-07: an agent echoed this token value into a session transcript through a `${VAR:-default}` expansion, so revoke the token at GitHub rather than only delete the rc line ATTRIBUTED 2026-08-10, so the NEXT STEP above is now DONE: all ten surviving macOS FAILs were read individually from the public annotations endpoint — (1) Context compactor selftest, (2) Repository work-boundary aggregate selftest (IMP-103 / G129), (3) Competitive docs selftest, (4) Capability freshness selftest, (5) Capability ledger selftest, (6) MCP trust-boundary selftest (IMP-102 / SCOPE-7), (7) MCP HTTP transport selftest (v6.1 / R9), (8) v5.3 downstream-install selftest (G1), (9) Experience-recall indexer selftest (IMP-037 / SCOPE-2), (10) BSD-userland simulator selftest. TWO ARE FIXED. (10) the simulator's OWN selftest skipped only when a tool was ABSENT, so on macOS — where sed/date/stat are present but BSD — it ran where it structurally cannot succeed, since the simulator rewrites an accepted BSD spelling into GNU form and then calls the real binary and therefore needs GNU coreutils underneath; it is now guarded on CAPABILITY (GNU sed/date accept --version, BSD reject it) per the never-branch-on-uname policy, landed in b902a99, verified by a GNU baseline that still runs every assertion plus two adversarial shadows that force the skip. (9) the indexer selftest fabricated a binding packet whose repositoryRoot was UNRESOLVED while production canonicalizes it (repository-binding.sh via cd -P/pwd -P) and the indexer stores candidate.resolve(strict=True) and compares the two as exact strings; on macOS TMPDIR is a symlink (/var to /private/var) so every packet was rejected as invalid-repository-packet — fixed in 619deaf, going from 47 passed/3 failed to 50/0 under a symlinked TMPDIR with the normal-TMPDIR control unchanged at 50/0. METHOD CORRECTION, MEASURED 2026-08-10: the instruction earlier in this cell to reproduce a survivor locally with the BSD userland simulator is NECESSARY BUT NOT SUFFICIENT and MUST NOT be treated as a clearing test. Measured on this host: the sealed simulator reproduced 0 of 8; a symlinked TMPDIR reproduced exactly 1 (the experience-recall indexer); shadowing awk with mawk reproduced 0; simulator plus symlinked TMPDIR together reproduced 0; and missing Python dependencies explain nothing for four of them because context-compactor, competitive-docs, capability-freshness and capability-ledger import no Python at all. A simulator PASS therefore does NOT rule a selftest out, and any earlier RULED OUT note resting only on a simulator pass is over-claimed. ATTRIBUTED AND FIXED 2026-08-10 (second pass), (8) v5.3 downstream-install: the earlier NOT EVALUATED note is superseded. The local lock collision it describes is REAL but is a LOCAL-ONLY artifact and is NOT the CI cause, because on macOS `flock` is absent so the lock is never taken and no collision is possible there. The CI cause is the flock-absent degrade NOTE itself: the guard prints a THREE-line `NOTE: flock not found` to stderr BEFORE the banner, and both T1 and T2 captured `2>&1 | head -5` and grepped that window for `Install mode:`, which the 3-line preamble pushes to line 6 — outside the window. Confirmed by the annotation text, which literally reads `(head: NOTE: flock not`. Reproduced locally against a control: with no preamble the marker is found, with a 3-line stderr preamble it is not. Fixed by widening both windows to `head -30`, which still SIGPIPEs the run early so the check stays cheap; the fix is non-tautological because the old 5-line window still fails the same reproduction while the new one passes. T1 carried the identical latent flaw and was fixed in the same change even though CI had only surfaced T2. Verified green under the conditions framework-validate actually uses (nested, `BUBBLES_FRAMEWORK_VALIDATE_LOCK_HELD` inherited): T1 through T5 all pass, exit 0. Standalone invocation of this selftest remains broken by the lock collision described above and is a separate, local-only wart. ROOT CAUSE PROVEN 2026-08-10, ONE cause explains FOUR survivors — context-compactor, repository work-boundary (as a downstream canary of the compactor), competitive-docs and capability-freshness: `bubbles/scripts/context-compactor.sh`, `docs-registry-resolve.sh`, `generate-capability-ledger-docs.sh` and `developer-profile.sh` each open with `if command -v gawk >/dev/null 2>&1; then awk() { command gawk "$@"; }; fi` and then use the GNU-only THREE-ARGUMENT `match(str, re, arr)` at 25 sites (2 / 13 / 7 / 3). The shim self-heals ONLY where gawk exists. `.github/workflows/agnosticity.yml` installs `bash` on the macOS runner and never gawk, so on macOS every 3-arg match is a syntax error, awk emits nothing, and the caller silently reads EMPTY fields rather than failing — context-compactor returns an envelope whose every extracted field is null while `timestamp` and `rawPointer` still populate, which is exactly the CI assertion set. WHY THE EARLIER SIMULATOR PASS WAS A FALSE CLEAR: the sealed BSD simulator runs on a Linux host that HAS gawk, so the shim fired inside it and masked the defect; and the note recording that "shadowing awk with mawk reproduced 0" was structurally INERT, because a bash FUNCTION named awk overrides any PATH shadow, so that experiment could never have reproduced anything. Both prior RULED OUT notes covering these four are therefore over-claimed and are withdrawn. REPRODUCTION THAT WORKS, controlled A/B on one variable: build a sealed PATH dir containing every needed binary EXCEPT gawk, symlinking awk to mawk (mawk rejects 3-arg match identically to BSD awk); arm A without gawk reproduced the CI assertions byte-identically (context-compactor 6 failures, competitive-docs exactly 2, capability-freshness exit 2 with no FAIL lines, which is why its annotation carried no detail), arm B with gawk added to the SAME dir passed every one at exit 0. FIX NOT ATTEMPTED HERE: the honest options are (a) rewrite all 25 sites to POSIX two-arg `match` plus `substr`/`RSTART`/`RLENGTH`, the only option that satisfies this repo's never-branch-on-uname portability policy, (b) install gawk on the macOS runner, which fixes CI but leaves every downstream macOS operator silently corrupted, or (c) at minimum make the shim FAIL LOUD when gawk is absent, converting silent null-field corruption into an attributable error. (a) is the correct durable fix and is scoped as real work, not a one-line patch. STILL UNATTRIBUTED AFTER THIS PASS: capability-ledger, which fails identically WITH and WITHOUT gawk and therefore has a separate cause, plus the MCP http-transport and trust-boundary pair whose annotations both read as a server that never came up. NEXT STEP for those six: the annotation names WHICH check failed but not WHY, and the raw job log stays 403 admin-only, so the options are to run them on real macOS hardware or to extend bubbles_ci_annotate_failure to carry the failing assertion lines; run_check in framework-validate.sh currently STREAMS each check's output without capturing it, so capturing it (tee plus PIPESTATUS to preserve the exit status) is the enabling change. GAWK ROOT CAUSE FIXED 2026-08-10: all 21 GNU 3-arg `match()` sites across all four shim-carrying scripts are rewritten to POSIX (context-compactor 1, developer-profile 2, docs-registry-resolve 12, generate-capability-ledger-docs 6) and every shim is deleted; 0 live shims remain repo-wide. Evidence on a sealed gawk-free PATH (awk -> mawk): context-compactor-selftest 6 FAIL -> 0 FAIL/14 PASS; competitive-docs-selftest 2 FAIL -> 0; capability-freshness-selftest exit 2 -> 0; docs-registry-resolve output byte-identical to the gawk reference across all three modes. Non-tautological: the OLD form still dies under mawk with "syntax error at or near ," emitting 200 bytes instead of the 1462-byte registry. CORRECTION to the sentence above: capability-ledger does NOT "fail identically WITH and WITHOUT gawk". It is NON-DETERMINISTIC — two consecutive runs on an UNMODIFIED tree gave WITH-gawk fails=1 then fails=0 — so its cause is order/state dependence, not gawk, and it stays open. The MCP http-transport and trust-boundary pair were addressed separately by 5190df3. | 2026-08-03 | 2026-08-10 |
| OW-012 | Gate-hit telemetry now records which gates ever reject anything, but no gate has been retired on that evidence yet, and 26 declared gates are still named by no script at all | residue | bubbles/scripts/gate-hit-log.sh (landed IMP-036 SCOPE-4, logging half only). The 26 script-less ids measured 2026-08-05: G011-G017, G023, G030, G032, G033, G036, G050, G054, G062, G065, G066, G071, G079, G081, G112-G114, G116, G117, G119. First live sample over one guard run showed 29 gates observed and 20 with zero rejections | open | gate registry owner | Wait until the log carries at least 60 days across the consuming repos, then run `bash bubbles/scripts/gate-hit-log.sh report` per repo and retire every gate with zero recorded rejections. Do NOT retire on the first sample: a gate that has not yet rejected anything may simply not have been exercised, and one guard run against one spec is not evidence of uselessness. CORRECTED 2026-08-07 — the second half of this item was a MISMEASUREMENT and is now withdrawn. "26 declared gates named by no script at all" was produced by grepping bubbles/scripts/ for each gate id, which counts every NON-SCRIPT enforcement surface as absence. Read from the registry's own enforcedBy field instead: all 114 gates declare one, the surfaces are script 89 / mode-required 35 / guard-check 23 / behavioral 2 / unbound 2, every `script:` binding resolves to a file that exists, and `guard-check:6B` for G066 is present in state-transition-guard. Exactly TWO gates are `unbound` (G070, G071) and the schema says that value exists precisely so an unenforced gate stays visible rather than hiding behind a plausible-looking binding. There is no 26-gate wiring backlog; do not recreate one from a grep. DUE: 2026-10-04 - 60 days after the 2026-08-05 telemetry baseline. On that date run `bash bubbles/scripts/gate-hit-log.sh report` in each of the six consuming repos and compare gates with zero recorded rejections against gates never exercised; retire only the former | 2026-08-06 | 2026-08-07 |
| OW-014 | IMP-036 landed all eight scopes but none of its outcome metrics has been re-measured, so the improvement is asserted by construction rather than observed | residue | IMP-036 was deleted on completion; the full rationale for each scope is in its commit message, and the baselines were measured 2026-08-05 across six delivery repos | open | framework health owner | Re-measure six numbers around 2026-10-06 using the method recorded in the IMP-036 commit messages. (1) parent-expanded occurrences in NEWLY written state: baseline 3,951 total, target 0 for new runs, read from `gate-hit-log.sh report` which now prints runs-using-parent-expansion. (2) bugs filed against an already-done parent spec: baseline 69 percent (813 of 1179), target under 40. (3) product share of changed lines: baseline 28 percent, target over 45. (4) state-file commits per repo per 60 days: baseline 306-563, target at least halved. (5) framework always-on context: baseline 850 lines, now 545. (6) GUARDRAIL, the fixed-30-day-window spec completion rate: baseline 45 percent for the June cohort, and if it FELL then something load-bearing was cut and must be restored before continuing. Do not report an improvement without its denominator; asserting one without measuring is the exact failure IMP-036 was written to correct. DUE: 2026-10-06 - the 60-day post-landing window. Measure metric 6 FIRST: if the completion rate fell below the 45 percent June baseline, stop and restore what was cut before reading any other number, because the remaining five would then be measuring a regression | 2026-08-06 | 2026-08-07 |
